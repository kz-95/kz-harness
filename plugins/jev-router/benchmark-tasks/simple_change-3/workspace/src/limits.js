// The limits an upload is held to.

/** The largest file a person may upload, in bytes. */
export const MAX_UPLOAD_BYTES = 10 * 1024 * 1024

/** The most files one person may keep. */
export const MAX_FILES_PER_PERSON = 10

/** File types that are accepted. */
export const ACCEPTED_TYPES = ['image/png', 'image/jpeg', 'application/pdf']
