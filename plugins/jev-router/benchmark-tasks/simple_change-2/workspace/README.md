# linecount

Counts the lines of a text file, leaving blank lines out unless asked.

    node src/cli.js [options] <file>

- `--verbose` prints each step as it runs.
- `--blank` counts blank lines too.
- `--help` prints the usage text.
